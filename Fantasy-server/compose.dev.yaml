services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: fantasy
      POSTGRES_USER: fantasy
      POSTGRES_PASSWORD: fantasy
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  fantasy.auth:
    image: fantasy.auth
    build:
      context: .
      dockerfile: Fantasy.Auth/dev.dockerfile
    ports:
      - "8080:8080"
    environment:
      - ConnectionStrings__Database=Host=postgres;Port=5432;Database=fantasy;Username=fantasy;Password=fantasy
      - ConnectionStrings__Redis=redis:6379
    volumes:
      - .:/src
    depends_on:
      - postgres
      - redis

  fantasy.game:
    image: fantasy.game
    build:
      context: .
      dockerfile: Fantasy.Game/dev.dockerfile
    ports:
      - "8081:8080"
    environment:
      - ConnectionStrings__Database=Host=postgres;Port=5432;Database=fantasy;Username=fantasy;Password=fantasy
      - ConnectionStrings__Redis=redis:6379
    volumes:
      - .:/src
    depends_on:
      - postgres
      - redis

volumes:
  postgres_data:
